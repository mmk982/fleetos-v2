/**
 * Monthly Executed Forms — per-vessel checklist + submissions
 * (`PROJECT_PLAN.md` §10).
 *
 * Requirements admin currently lives on the list page; move to Settings or
 * the vessel profile once those exist.
 */
