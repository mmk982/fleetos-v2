/**
 * Drawings module — vessel-scoped technical drawings (`PROJECT_PLAN.md` §11).
 *
 * Categories are seed + read-only; Settings CRUD comes later. Attachments
 * use the generic `/api/attachments/[id]` resolver (no access_logs).
 */
